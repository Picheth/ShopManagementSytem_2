import React from "react";

const ContactModule: React.FC = () => {
	return (
		<div>
			<h2>Contacts</h2>
			{/* Add contact list, import/export, form, etc. here */}
		</div>
	);
};

export default ContactModule;
